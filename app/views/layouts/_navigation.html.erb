<%= link_to "PhotoApp", root_path, :class => 'brand' %>
<ul class="nav">
  <% if user_signed_in? %>
    <li>
      <%= link_to 'Logout', destroy_user_session_path, :method=>'delete' %>
    </li>
  <% else %>
    <li>
      <%= link_to 'Login', new_user_session_path %>
    </li>
  <% end %>
  <% if user_signed_in? %>
    <li>
      <%= link_to 'Edit account', edit_user_registration_path %>
    </li>
  <% else %>
    <li>
      <%= link_to 'Sign up', new_user_registration_path %>
    </li>
    <li>
  <%#= link_to 'Associate my account', associate_photos_path %>
    </li>
  <% end %>
</ul>
<% unless user_signed_in? %>
  <%if params[:taker_id].present?%>
    <%= form_tag(associate_photos_path(:taker_id => params[:taker_id]), :method => :post, :authenticity_token => false,  :class => "navbar-form pull-right") do -%>
      <%=text_field_tag 'email', nil, :class => "input-medium", :placeholder => 'Enter login email here', :required => true-%><%=submit_tag 'Associate my account', :class => 'btn'-%>
    <%end%>
  <%end%>
<%end%>